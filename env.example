# Firebase Configuration (Client-Side)
# Copy this file to .env.local and fill in your actual Firebase values
# 
# IMPORTANT: These are PUBLIC configuration values, NOT secret keys!
# They are intentionally exposed to the browser for Firebase client SDK to work.
# You can find these values in your Firebase Console > Project Settings > General > Your apps

# Required for Firebase to work:
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef123456

# Optional: Analytics (only if you want Firebase Analytics)
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Security Note: 
# - These variables are PUBLIC and safe to expose in the browser
# - Firebase client SDK needs them to connect to your project
# - No secret keys are needed for client-side Firebase apps
# - If you later add server-side features, you'll need separate secret keys

# Stripe Configuration (Client-Side)
# Get these from your Stripe Dashboard
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Stripe Price IDs (Client-Side - Required for production)
# Create these products in your Stripe Dashboard and copy the price IDs
NEXT_PUBLIC_STRIPE_SINGLE_AUDIT_PRICE_ID=price_your_single_audit_price_id
NEXT_PUBLIC_STRIPE_FIVE_AUDITS_PRICE_ID=price_your_five_audits_price_id
NEXT_PUBLIC_STRIPE_TEN_AUDITS_PRICE_ID=price_your_ten_audits_price_id

# Stripe Configuration (Server-Side - Keep Secret!)
# Add these to your Vercel environment variables, NOT in .env.local
# STRIPE_SECRET_KEY=sk_test_your_secret_key_here
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# GitHub OAuth Configuration (Client-Side)
# Get these from your GitHub OAuth App settings
# These are PUBLIC and safe to expose in the browser
NEXT_PUBLIC_GITHUB_CLIENT_ID=your_github_oauth_client_id_here
NEXT_PUBLIC_GITHUB_REDIRECT_URI=http://localhost:9002/auth/github/callback
NEXT_PUBLIC_APP_URL=http://localhost:9002

# GitHub OAuth Configuration (Server-Side - Keep Secret!)
# Add this to your Vercel environment variables
# GITHUB_CLIENT_SECRET=your_github_oauth_client_secret_here

# OpenAI Configuration (Server-Side - Keep Secret!)
# Add this to your Vercel environment variables for the worker service
# OPENAI_API_KEY=your_openai_api_key_here

# Firebase Admin Configuration (Server-Side - Keep Secret!)
# These are for the worker service and should be added to your worker environment
# You can find these in your Firebase Console > Project Settings > Service Accounts
# FIREBASE_PROJECT_ID=your_firebase_project_id
# FIREBASE_PRIVATE_KEY_ID=your_private_key_id
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
# FIREBASE_CLIENT_EMAIL=your_service_account_email@project.iam.gserviceaccount.com
# FIREBASE_CLIENT_ID=your_client_id
# FIREBASE_AUTH_URI=https://accounts.google.com/o/oauth2/auth
# FIREBASE_TOKEN_URI=https://oauth2.googleapis.com/token
# FIREBASE_AUTH_PROVIDER_X509_CERT_URL=https://www.googleapis.com/oauth2/v1/certs
# FIREBASE_CLIENT_X509_CERT_URL=https://www.googleapis.com/robot/v1/metadata/x509/your_service_account_email%40project.iam.gserviceaccount.com

# GitHub Configuration (Server-Side - Keep Secret!)
# Add this to your worker environment if you need to access private repositories
# GITHUB_TOKEN=your_github_personal_access_token

# Worker Configuration (Server-Side - Keep Secret!)
# These are for the Google Cloud Run worker service
# WORKER_PORT=8080
# WORKER_ENV=production
