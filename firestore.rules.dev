rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules allow all access - ONLY use for development/testing!
    // NEVER deploy these rules to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
